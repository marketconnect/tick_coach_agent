from langchain_core.tools import tool
from typing import List

@tool
def finalize_answer(draft: str) -> str:
    """Вызови ПЕРЕД финальным ответом, чтобы показать пользователю черновик и получить подтверждение/правки."""
    return draft

@tool
def ask_clarifying_questions(questions: List[str]) -> str:
    """Задай пользователю список уточняющих вопросов, чтобы прояснить его запрос.
    Используй это, только если запрос неясен, и ты не можешь продолжить без дополнительной информации.
    Сгруппируй все вопросы в один список."""
    # Возвращаемое значение этого инструмента не отправляется пользователю. Оно предназначено для контекста агента.
    # Граф прервется и обработает отправку вопросов пользователю.
    return "Уточняющие вопросы подготовлены для пользователя."
